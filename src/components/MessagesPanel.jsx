import { useEffect, useMemo, useRef, useState } from "react";
import { Check, ChevronLeft, ImagePlus, MessageCircle, Plus, Search, Send, Users, X, Smile, Mic, Square, Reply, Trash2, Play, Pause, Phone, Video, Pencil, MoreVertical } from "lucide-react";
import EmojiPicker from "emoji-picker-react";
import { getConversations, getConversationDetails, getMessages, sendMessage, createConversation, subscribeToConversation, markMessageAsRead, addMessageReaction, removeMessageReaction, deleteMessage, consumePendingDirectConversationId } from "../lib/messages";
import { uploadMessageMedia, uploadVoiceMessage } from "../lib/storage";
import { supabase } from "../lib/supabase";
import { VerifiedBadge } from "./VerifiedBadge";
import "./MessagesPanel.css";
import "./MessagesPanel.chat-fix.css";

const REACTIONS = ["❤️", "😊", "😂", "💪", "👍"];
function conversationTitle(c, userId) { if (!c) return "Conversation"; if (c.type === "group") return c.name || "Group conversation"; const member = c.conversation_members?.find(m => m.user_id !== userId); return member?.profiles?.display_name || member?.profiles?.username || c._direct_profile?.display_name || c._direct_profile?.username || c.name || "Direct conversation"; }
function ConversationAvatar({ conversation, size = 38 }) { const profile = conversation?.type === "direct" ? conversation?._direct_profile : null; return <div className="conversation-avatar" style={{ width: size, height: size, flex: `0 0 ${size}px` }}>{profile?.avatar_url ? <img src={profile.avatar_url} alt="" /> : conversation?.type === "group" ? <Users size={18} /> : <MessageCircle size={18} />}</div>; }
function VoiceNote({ src }) { const ref = useRef(null); const [playing, setPlaying] = useState(false); const [duration, setDuration] = useState(0); const [current, setCurrent] = useState(0); useEffect(() => { const a = ref.current; if (!a) return; const loaded=()=>setDuration(Number.isFinite(a.duration)?a.duration:0), time=()=>setCurrent(a.currentTime||0), ended=()=>{setPlaying(false);setCurrent(0)}; a.addEventListener("loadedmetadata",loaded);a.addEventListener("timeupdate",time);a.addEventListener("ended",ended);return()=>{a.removeEventListener("loadedmetadata",loaded);a.removeEventListener("timeupdate",time);a.removeEventListener("ended",ended)}},[src]); const toggle=async()=>{const a=ref.current;if(!a)return;try{if(a.paused){await a.play();setPlaying(true)}else{a.pause();setPlaying(false)}}catch(_){}};const format=v=>`${Math.floor(v/60)}:${String(Math.floor(v%60)).padStart(2,"0")}`;const pct=duration?Math.min(100,Math.max(0,current/duration*100)):0;return <div className="voice-note" onClick={e=>e.stopPropagation()}><audio ref={ref} src={src} preload="metadata"/><button type="button" className="voice-note-play" onClick={toggle}>{playing?<Pause size={15}/>:<Play size={15}/>}</button><div className="voice-note-main"><div className="voice-note-track"><span className="voice-note-progress" style={{width:`${pct}%`}}/></div><span className="voice-note-time">{format(playing?current:duration)}</span></div></div>; }

export function MessagesPanel({ userId, initialConversationId = null, onBack, onChatOpen }) {
  const [conversations,setConversations]=useState([]); const [selectedId,setSelectedId]=useState(null); const [details,setDetails]=useState(null); const [messages,setMessages]=useState([]); const [draft,setDraft]=useState(""); const [search,setSearch]=useState(""); const [loading,setLoading]=useState(true); const [chatLoading,setChatLoading]=useState(false); const [sending,setSending]=useState(false); const [uploading,setUploading]=useState(false); const [recording,setRecording]=useState(false); const [error,setError]=useState(""); const [replyingTo,setReplyingTo]=useState(null); const [reactionMessageId,setReactionMessageId]=useState(null); const [messageMenuId,setMessageMenuId]=useState(null); const [editingMessageId,setEditingMessageId]=useState(null); const [editingDraft,setEditingDraft]=useState(""); const [newChatOpen,setNewChatOpen]=useState(false); const [newMemberId,setNewMemberId]=useState(""); const [remoteOnline,setRemoteOnline]=useState(false); const [emojiOpen,setEmojiOpen]=useState(false);
  const fileRef=useRef(null), recorderRef=useRef(null), chunksRef=useRef([]), streamRef=useRef(null), messageStreamRef=useRef(null), presenceRef=useRef(null), composerRef=useRef(null), swipeRef=useRef({id:null,x:0,y:0,active:false}), longPressRef=useRef({timer:null,id:null});
  const beginSwipeReply=(e,id)=>{const t=e.touches?.[0];if(t)swipeRef.current={id,x:t.clientX,y:t.clientY,active:false}}; const moveSwipeReply=e=>{const t=e.touches?.[0],s=swipeRef.current;if(!t||!s.id)return;const dx=t.clientX-s.x,dy=Math.abs(t.clientY-s.y);if(dx>18&&dx>dy*1.25)s.active=true}; const endSwipeReply=(e,message)=>{const t=e.changedTouches?.[0],s=swipeRef.current;if(!t||!s.id)return;const dx=t.clientX-s.x,dy=Math.abs(t.clientY-s.y);if(dx>=70&&dx>dy*1.25){setReplyingTo(message);setReactionMessageId(null);setMessageMenuId(null)}swipeRef.current={id:null,x:0,y:0,active:false}};
  const clearLongPress=()=>{if(longPressRef.current.timer)clearTimeout(longPressRef.current.timer);longPressRef.current={timer:null,id:null}};
  const startLongPress=(message,e)=>{clearLongPress();if(message.sender_id!==userId)return;longPressRef.current.id=message.id;longPressRef.current.timer=setTimeout(()=>{setMessageMenuId(message.id);setReactionMessageId(null);e?.preventDefault?.()},520)};
  const cancelLongPress=()=>clearLongPress();
  useEffect(()=>()=>clearLongPress(),[]);
  useEffect(()=>{if(onChatOpen)onChatOpen(Boolean(selectedId));return()=>{if(onChatOpen)onChatOpen(false)}},[selectedId,onChatOpen]);
  const loadConversations=async()=>{if(!userId)return;try{setLoading(true);setError("");const data=await getConversations(userId);const enriched=await Promise.all((data||[]).map(async c=>{let latest=null;try{const{data:row}=await supabase.from("messages").select("content,message_type,created_at,is_deleted").eq("conversation_id",c.id).order("created_at",{ascending:false}).limit(1).maybeSingle();latest=row||null}catch(_){}return{...c,_latest_message:latest}}));setConversations(enriched);const pending=initialConversationId||consumePendingDirectConversationId();if(pending)setSelectedId(pending)}catch(e){setError(e?.message||"Unable to load conversations.")}finally{setLoading(false)}};
  useEffect(()=>{loadConversations()},[userId,initialConversationId]); useEffect(()=>{if(!userId)return;const t=setInterval(()=>loadConversations().catch(()=>{}),5000);return()=>clearInterval(t)},[userId]);
  useEffect(()=>{if(!selectedId){setDetails(null);setMessages([]);return}let cancelled=false;(async()=>{try{setChatLoading(true);setError("");const conversation=await getConversationDetails(selectedId);if(!conversation)throw new Error("Conversation could not be found.");if(cancelled)return;setDetails(conversation);try{const data=await getMessages(selectedId,100,userId);if(!cancelled)setMessages(Array.isArray(data)?data:[])}catch(e){if(!cancelled)setError(e?.message||"Messages could not be loaded")} }catch(e){if(!cancelled){setError(e?.message||"Unable to open conversation.");setSelectedId(null)}}finally{if(!cancelled)setChatLoading(false)}})();return()=>{cancelled=true}},[selectedId,userId]);
  useEffect(()=>{if(!selectedId)return;return subscribeToConversation(selectedId,async incoming=>{let enriched=incoming;if(incoming.reply_to_id){try{const { data: reply }=await supabase.from("messages").select(`id, content, message_type, media_url, is_deleted, sender_id, created_at, profiles:sender_id(id, username, display_name, avatar_url)`).eq("id",incoming.reply_to_id).maybeSingle();enriched={...incoming,reply_to:reply||null}}catch(_){}}setMessages(prev=>prev.some(m=>m.id===enriched.id)?prev:[...prev,enriched].sort((a,b)=>new Date(a.created_at)-new Date(b.created_at)));setConversations(prev=>prev.map(c=>c.id===selectedId?{...c,_latest_message:enriched,updated_at:enriched.created_at,last_message_at:enriched.created_at,unread_count:0}:c));if(enriched.sender_id!==userId)markMessageAsRead(enriched.id,userId).catch(()=>{})},updated=>setMessages(prev=>prev.map(m=>m.id===updated.id?{...m,...updated}:m)) )},[selectedId,userId]);
  useEffect(()=>{if(!messageStreamRef.current||!selectedId)return;requestAnimationFrame(()=>{const el=messageStreamRef.current;if(el)el.scrollTop=el.scrollHeight})},[messages.length,selectedId,chatLoading]);
  useEffect(()=>{if(!selectedId||!userId)return;const channel=supabase.channel(`convogram-presence-${selectedId}`,{config:{presence:{key:userId}}});presenceRef.current=channel;const sync=()=>{const users=Object.values(channel.presenceState()).flat();setRemoteOnline(users.some(u=>u.userId&&u.userId!==userId&&u.online!==false))};channel.on("presence",{event:"sync"},sync).on("presence",{event:"join"},sync).on("presence",{event:"leave"},sync).subscribe(async status=>{if(status==="SUBSCRIBED"){await channel.track({userId,online:true});sync()}});return()=>{channel.untrack().catch(()=>{});supabase.removeChannel(channel);presenceRef.current=null;setRemoteOnline(false)}},[selectedId,userId]);
  const filtered=useMemo(()=>{const q=search.trim().toLowerCase();if(!q)return conversations;return conversations.filter(c=>`${conversationTitle(c,userId)} ${c.type||""}`.toLowerCase().includes(q))},[conversations,search,userId]); const sorted=useMemo(()=>[...filtered].sort((a,b)=>new Date(b?._latest_message?.created_at||b?.updated_at||0)-new Date(a?._latest_message?.created_at||a?.updated_at||0)),[filtered]); const displayPreview=c=>{const m=c?._latest_message;if(!m)return c.type==="group"?"Group":"Private chat";if(m.is_deleted)return"Message deleted";if(m.message_type==="image")return"Photo";if(m.message_type==="video")return"Video";if(m.message_type==="audio")return"Voice message";return m.content||"Attachment"};
  const handleSend=async e=>{e.preventDefault();const text=draft.trim();if(!text||!selectedId||!userId||sending)return;try{setSending(true);const sent=await sendMessage(selectedId,userId,text,"text",null,replyingTo?.id||null);setMessages(prev=>prev.some(m=>m.id===sent.id)?prev:[...prev,sent]);setDraft("");setReplyingTo(null);setEmojiOpen(false)}catch(e){setError(e?.message||"Could not send message.")}finally{setSending(false)}};
  const handleEmojiClick=emojiData=>{setDraft(prev=>prev+emojiData.emoji);requestAnimationFrame(()=>{composerRef.current?.focus()})};
  const handleMedia=async e=>{const file=e.target.files?.[0];if(!file||!selectedId||!userId)return;try{setUploading(true);const uploaded=await uploadMessageMedia(file,userId);const sent=await sendMessage(selectedId,userId,null,uploaded.mediaType,uploaded.url,replyingTo?.id||null);setMessages(prev=>[...prev,sent]);setReplyingTo(null)}catch(e){setError(e?.message||"Could not upload media.")}finally{setUploading(false);if(fileRef.current)fileRef.current.value=""}};
  const startRecording=async()=>{if(!navigator.mediaDevices?.getUserMedia){setError("Microphone is not available on this device.");return}try{const stream=await navigator.mediaDevices.getUserMedia({audio:true});streamRef.current=stream;const recorder=new MediaRecorder(stream);chunksRef.current=[];recorder.ondataavailable=e=>{if(e.data?.size)chunksRef.current.push(e.data)};recorder.onstop=async()=>{try{const blob=new Blob(chunksRef.current,{type:recorder.mimeType||"audio/webm"});const file=new File([blob],`voice-${Date.now()}.webm`,{type:blob.type||"audio/webm"});const uploaded=await uploadVoiceMessage(file,userId);const sent=await sendMessage(selectedId,userId,null,"audio",uploaded.url,replyingTo?.id||null);setMessages(prev=>[...prev,sent])}catch(e){setError(e?.message||"Could not send voice note.")}finally{stream.getTracks().forEach(t=>t.stop());setRecording(false)}};recorderRef.current=recorder;recorder.start();setRecording(true)}catch(e){setError(e?.message||"Microphone permission is required.")}}; const stopRecording=()=>{if(recorderRef.current&&recorderRef.current.state!=="inactive")recorderRef.current.stop();else setRecording(false)};
  const toggleReaction=async(message,reaction)=>{if(!userId)return;const mine=(message.message_reactions||[]).find(r=>r.user_id===userId&&r.reaction===reaction);try{if(mine)await removeMessageReaction(message.id,userId,reaction);else await addMessageReaction(message.id,userId,reaction);const data=await getMessages(selectedId,100,userId);setMessages(data||[])}catch(e){setError(e?.message||"Could not update reaction.")}finally{setReactionMessageId(null);setMessageMenuId(null)}};
  const handleDelete=async message=>{if(message.sender_id!==userId)return;try{const updated=await deleteMessage(message.id);setMessages(prev=>prev.map(m=>m.id===updated.id?{...m,...updated}:m))}catch(e){setError(e?.message||"Could not delete message.")}finally{setReactionMessageId(null);setMessageMenuId(null)}};
  const beginEdit=message=>{if(message.sender_id!==userId||message.message_type!=="text"||message.is_deleted)return;setEditingMessageId(message.id);setEditingDraft(message.content||"");setMessageMenuId(null);setReactionMessageId(null);setDraft(message.content||"");requestAnimationFrame(()=>composerRef.current?.focus())};
  const saveEdit=async()=>{const text=editingDraft.trim();if(!editingMessageId||!text)return;try{const {data,error:editError}=await supabase.from("messages").update({content:text,updated_at:new Date().toISOString()}).eq("id",editingMessageId).eq("sender_id",userId).select().single();if(editError)throw editError;setMessages(prev=>prev.map(m=>m.id===editingMessageId?{...m,...data,content:text}:m));setEditingMessageId(null);setEditingDraft("");setDraft("");setError("")}catch(e){setError(e?.message||"Could not edit message.")}};
  const cancelEdit=()=>{setEditingMessageId(null);setEditingDraft("");setDraft("")};
  const handleCreateChat=async e=>{e.preventDefault();const memberId=newMemberId.trim();if(!memberId)return;try{const conversation=await createConversation(userId,"direct",null,null,[memberId]);setNewChatOpen(false);setNewMemberId("");setSelectedId(conversation.id);await loadConversations()}catch(e){setError(e?.message||"Could not create chat.")}};
  const backToChats=()=>{setSelectedId(null);setDetails(null);setMessages([]);setReplyingTo(null);setReactionMessageId(null);setMessageMenuId(null);setEmojiOpen(false);cancelEdit();if(onBack)onBack()};
  const remoteProfile=details?.conversation_members?.find(m=>m.user_id!==userId)?.profiles||null; const titleConversation=details?{...details,_direct_profile:remoteProfile}:null;
  return <div className={`messages-panel ${selectedId?"chat-open":""}`}>{error&&<div className="messages-error">{error}<button type="button" onClick={()=>setError("")}><X size={15}/></button></div>}{!selectedId&&<aside className="conversation-list"><div className="messages-list-head"><h2>Messages</h2><button type="button" className="messages-icon-button" onClick={()=>setNewChatOpen(true)}><Plus size={19}/></button></div><div className="messages-search"><Search size={16}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search chats"/></div>{loading?<div className="messages-empty">Loading chats...</div>:sorted.length?sorted.map(c=><button type="button" key={c.id} className="conversation-item" onClick={()=>{setError("");setSelectedId(c.id)}}><ConversationAvatar conversation={c}/><div className="conversation-copy"><strong>{conversationTitle(c,userId)}{c._direct_profile?.is_verified&&<VerifiedBadge verified size={15}/>}</strong><span>{displayPreview(c)}</span></div>{Number(c.unread_count)>0&&<span className="conversation-unread-badge">{c.unread_count>99?"99+":c.unread_count}</span>}</button>):<div className="messages-empty"><MessageCircle size={28}/><p>No chats yet.</p><button type="button" className="secondary-button" onClick={()=>setNewChatOpen(true)}>Start a chat</button></div>}</aside>}
    {selectedId&&<section className="chat-window">{chatLoading&&!details?<div className="chat-loading">Opening messages…</div>:details?<><header className="chat-head"><button type="button" className="chat-back-button" onClick={backToChats}><ChevronLeft size={20}/></button><ConversationAvatar conversation={titleConversation} size={38}/><div className="chat-head-copy"><strong>{conversationTitle(titleConversation,userId)}</strong><span>{remoteOnline?"Online":"Offline"}</span></div><div className="chat-call-actions"><button type="button" className="messages-icon-button" onClick={()=>window.dispatchEvent(new CustomEvent("convogram-start-call",{detail:{type:"voice"}}))} disabled={!remoteProfile?.id}><Phone size={17}/></button><button type="button" className="messages-icon-button" onClick={()=>window.dispatchEvent(new CustomEvent("convogram-start-call",{detail:{type:"video"}}))} disabled={!remoteProfile?.id}><Video size={17}/></button></div></header><div ref={messageStreamRef} className="message-stream" onClick={()=>{setMessageMenuId(null);setReactionMessageId(null)}}>{messages.length?messages.map(item=>{const mine=item.sender_id===userId;const media=["image","video","audio"].includes(item.message_type);const reactions=item.message_reactions||[];const menuOpen=messageMenuId===item.id;const reactionOpen=reactionMessageId===item.id;const replyPreview=item.reply_to;const replyName=replyPreview?.profiles?.display_name||replyPreview?.profiles?.username||"Reply";const replyText=replyPreview?.is_deleted?"Message deleted":replyPreview?.message_type==="image"?"Photo":replyPreview?.message_type==="video"?"Video":replyPreview?.message_type==="audio"?"Voice message":replyPreview?.content||"Attachment";return <div key={item.id} className={`message-row ${mine?"mine":"theirs"}`} onTouchStart={e=>{beginSwipeReply(e,item.id);startLongPress(item,e)}} onTouchMove={e=>{moveSwipeReply(e);if(Math.abs((e.touches?.[0]?.clientX||0)-(swipeRef.current.x||0))>12)cancelLongPress()}} onTouchEnd={e=>{cancelLongPress();endSwipeReply(e,item)}} onMouseDown={e=>startLongPress(item,e)} onMouseUp={cancelLongPress} onMouseLeave={cancelLongPress} onContextMenu={e=>{if(mine){e.preventDefault();setMessageMenuId(item.id)}}}><div className={`message-bubble ${menuOpen||reactionOpen?"reaction-active":""}`}>{replyPreview&&<div className="message-reply-preview"><div><strong>{replyName}</strong><span>{replyText}</span></div></div>}{editingMessageId===item.id?<div className="message-edit-inline"><input value={editingDraft} onChange={e=>setEditingDraft(e.target.value)} autoFocus/><div><button type="button" onClick={cancelEdit}>Cancel</button><button type="button" onClick={saveEdit}>Save</button></div></div>:item.is_deleted?<span className="message-text message-deleted">Message deleted</span>:media&&item.message_type==="audio"?<VoiceNote src={item.media_url}/>:media&&item.message_type==="image"?<img className="message-media" src={item.media_url} alt=""/>:media&&item.message_type==="video"?<video className="message-media" src={item.media_url} controls/>:<span className="message-text">{item.content}</span>}<small>{new Date(item.created_at).toLocaleTimeString([], {hour:"2-digit",minute:"2-digit"})}{mine&&<Check size={12}/>}</small></div>{menuOpen&&mine&&!item.is_deleted&&!editingMessageId&&<div className="message-actions"><div className="message-tools"><button type="button" title="Edit message" aria-label="Edit message" onClick={e=>{e.stopPropagation();beginEdit(item)}}><Pencil size={14}/><span>Edit</span></button><button type="button" title="Delete message" aria-label="Delete message" onClick={e=>{e.stopPropagation();handleDelete(item)}}><Trash2 size={14}/><span>Delete</span></button><button type="button" title="React to message" aria-label="React to message" onClick={e=>{e.stopPropagation();setMessageMenuId(null);setReactionMessageId(item.id)}}><Smile size={14}/><span>React</span></button></div></div>}{reactionOpen&&!item.is_deleted&&<div className="message-actions reaction-actions" onClick={e=>e.stopPropagation()}><div className="reaction-picker">{REACTIONS.map(r=><button key={r} type="button" onClick={()=>toggleReaction(item,r)}>{r}</button>)}</div></div>}</div>}) : <div className="messages-empty chat-empty"><MessageCircle size={30}/><p>No messages yet.</p><span>Start the conversation.</span></div>}</div>{editingMessageId&&<div className="reply-banner edit-banner"><div className="reply-banner-copy"><strong>Edit message</strong><span>Update your message and tap Save.</span></div><button type="button" className="messages-icon-button" title="Cancel edit" aria-label="Cancel edit" onClick={cancelEdit}><X size={16}/></button></div>}{!editingMessageId&&replyingTo&&<div className="reply-banner"><div className="reply-banner-copy"><strong>Replying to {replyingTo.profiles?.display_name||replyingTo.profiles?.username||"message"}</strong><span>{replyingTo.content||"Attachment"}</span></div><button type="button" className="messages-icon-button" title="Cancel reply" aria-label="Cancel reply" onClick={()=>setReplyingTo(null)}><X size={16}/></button></div>}<form className="message-composer" onSubmit={editingMessageId?(e=>{e.preventDefault();saveEdit()}):handleSend}><input ref={composerRef} value={editingMessageId?editingDraft:draft} onChange={e=>editingMessageId?setEditingDraft(e.target.value):setDraft(e.target.value)} placeholder={editingMessageId?"Edit message":"Message"}/>{editingMessageId?<><button type="button" className="messages-icon-button" title="Cancel edit" aria-label="Cancel edit" onClick={cancelEdit}><X size={19}/></button><button type="submit" className="send-button" disabled={!editingDraft.trim()}><Check size={18}/></button></>:<><button type="button" className={`messages-icon-button ${emojiOpen?"active":""}`} title="Emoji" aria-label="Emoji" onClick={()=>setEmojiOpen(v=>!v)}><Smile size={19}/></button><button type="button" className="messages-icon-button" title="Attach media" aria-label="Attach media" onClick={()=>fileRef.current?.click()} disabled={uploading}><ImagePlus size={19}/></button><input ref={fileRef} type="file" accept="image/*,video/*" hidden onChange={handleMedia}/>{recording?<button type="button" className="messages-icon-button active" title="Stop recording" aria-label="Stop recording" onClick={stopRecording}><Square size={18}/></button>:<button type="button" className="messages-icon-button" title="Voice message" aria-label="Voice message" onClick={startRecording}><Mic size={19}/></button>}<button type="submit" className="send-button" disabled={!draft.trim()||sending}><Send size={18}/></button></>}</form>{emojiOpen&&!editingMessageId&&<div className="convogram-emoji-panel"><EmojiPicker onEmojiClick={handleEmojiClick} width="100%" height="100%" previewConfig={{showPreview:false}}/></div>}</>:<div className="chat-loading">Loading…</div>}</section>}
    {newChatOpen&&<div className="new-chat-modal"><form onSubmit={handleCreateChat}><h3>Start a chat</h3><input value={newMemberId} onChange={e=>setNewMemberId(e.target.value)} placeholder="User ID"/><div><button type="button" onClick={()=>setNewChatOpen(false)}>Cancel</button><button type="submit">Start</button></div></form></div>}</div>;
}
