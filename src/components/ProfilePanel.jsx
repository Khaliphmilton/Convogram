import { useEffect, useMemo, useState } from "react";
import { Bookmark, Camera, Check, Grid3X3, Heart, Link2, Lock, MessageCircle, MoreHorizontal, PlaySquare, Plus, Settings, Share2, Sparkles, Tag, UserPlus, Users, X } from "lucide-react";
import { followUser, unfollowUser, isFollowing, getFollowers, getFollowing } from "../lib/profiles";
import { getSavedPosts, getLikedPosts, getTaggedPosts, getSavedPostIds, savePost, unsavePost, updateProfile } from "../lib/profile_features";

const tabs = [
  { id: "posts", label: "Posts", icon: Grid3X3 },
  { id: "shorts", label: "Shorts", icon: PlaySquare },
  { id: "saved", label: "Saved", icon: Bookmark },
  { id: "liked", label: "Liked", icon: Heart },
  { id: "tagged", label: "Tagged", icon: Tag },
  { id: "settings", label: "Settings", icon: Settings },
];

export function ProfilePanel({ profile, stats, posts = [], userId, onCreatePost, onCreateMoment, onMessage, onLogout, onProfileUpdated }) {
  const [active, setActive] = useState("posts");
  const [following, setFollowing] = useState(false);
  const [savedIds, setSavedIds] = useState(new Set());
  const [savedPosts, setSavedPosts] = useState([]);
  const [likedPosts, setLikedPosts] = useState([]);
  const [taggedPosts, setTaggedPosts] = useState([]);
  const [people, setPeople] = useState([]);
  const [peopleMode, setPeopleMode] = useState("");
  const [editOpen, setEditOpen] = useState(false);
  const [moreOpen, setMoreOpen] = useState(false);
  const [notice, setNotice] = useState("");
  const [saving, setSaving] = useState(false);
  const [edit, setEdit] = useState({ display_name: profile?.display_name || "", username: profile?.username || "", bio: profile?.bio || "", website: profile?.website || "", is_private: !!profile?.is_private });
  const ownPosts = useMemo(() => posts.filter((post) => post.user_id === profile?.id), [posts, profile?.id]);
  const mediaPosts = ownPosts.filter((post) => post.media_url);
  const displayPosts = active === "posts" ? ownPosts : active === "shorts" ? mediaPosts : active === "saved" ? savedPosts : active === "liked" ? likedPosts : taggedPosts;
  const likesCount = ownPosts.reduce((n, p) => n + (p.likes?.[0]?.count || 0), 0);
  function toast(text) { setNotice(text); window.clearTimeout(window.__cvProfileToast); window.__cvProfileToast = window.setTimeout(() => setNotice(""), 2400); }

  useEffect(() => { if (!userId || !profile?.id) return; (async () => { try { setFollowing(await isFollowing(userId, profile.id)); setSavedIds(new Set(await getSavedPostIds(userId))); } catch (err) { toast(err.message || "Unable to load profile state."); } })(); }, [userId, profile?.id]);
  useEffect(() => { if (!userId) return; (async () => { try { if (active === "saved") setSavedPosts(await getSavedPosts(userId)); if (active === "liked") setLikedPosts(await getLikedPosts(userId)); if (active === "tagged") setTaggedPosts(await getTaggedPosts(userId)); } catch (err) { toast(err.message || "Unable to load this profile section."); } })(); }, [active, userId]);
  useEffect(() => { if (profile) setEdit({ display_name: profile.display_name || "", username: profile.username || "", bio: profile.bio || "", website: profile.website || "", is_private: !!profile.is_private }); }, [profile]);

  async function toggleFollow() { if (!userId || userId === profile?.id) return; try { if (following) { await unfollowUser(userId, profile.id); setFollowing(false); toast("Unfollowed"); } else { await followUser(userId, profile.id); setFollowing(true); toast("Following"); } } catch (err) { toast(err.message || "Follow action failed."); } }
  async function toggleSave(postId) { try { if (savedIds.has(postId)) { await unsavePost(userId, postId); setSavedIds((prev) => new Set([...prev].filter((id) => id !== postId))); setSavedPosts((prev) => prev.filter((p) => p.id !== postId)); toast("Removed from Saved"); } else { await savePost(userId, postId); setSavedIds((prev) => new Set(prev).add(postId)); toast("Saved"); } } catch (err) { toast(err.message || "Could not update Saved."); } }
  async function openPeople(mode) { try { setPeopleMode(mode); setPeople(mode === "followers" ? await getFollowers(profile.id) : await getFollowing(profile.id)); } catch (err) { toast(err.message || "Could not load people."); } }
  async function saveProfile(e) { e.preventDefault(); setSaving(true); try { const updated = await updateProfile(userId, edit); onProfileUpdated?.(updated); setEditOpen(false); toast("Profile updated"); } catch (err) { toast(err.message || "Profile update failed."); } finally { setSaving(false); } }

  return <section className="profile-pro"><style>{` .profile-pro{max-width:1120px;margin:0 auto;padding-bottom:90px;color:#0b1220}.profile-pro-cover{height:270px;border-radius:34px;position:relative;overflow:hidden;background:radial-gradient(circle at 78% 22%,rgba(88,199,255,.7),transparent 27%),radial-gradient(circle at 20% 90%,rgba(47,124,255,.4),transparent 30%),linear-gradient(135deg,#050b16,#102d61 52%,#2877f2);box-shadow:0 28px 90px rgba(7,16,31,.3)}.profile-cover-grid{position:absolute;inset:-20%;opacity:.22;background-image:linear-gradient(rgba(255,255,255,.15) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,.15) 1px,transparent 1px);background-size:44px 44px;transform:perspective(600px) rotateX(58deg) rotateZ(-3deg);transform-origin:center bottom}.profile-cover-orb{position:absolute;width:300px;height:300px;border-radius:50%;right:11%;top:-110px;border:1px solid rgba(255,255,255,.35);box-shadow:0 0 100px rgba(88,199,255,.55),inset 0 0 60px rgba(88,199,255,.16)}.profile-cover-orb:after{content:"";position:absolute;inset:34px;border-radius:50%;border:1px solid rgba(255,255,255,.18)}.profile-cover-more{position:absolute;right:18px;top:18px;width:44px;height:44px;border:1px solid rgba(255,255,255,.22);background:rgba(4,10,20,.4);color:white;border-radius:15px;display:grid;place-items:center;backdrop-filter:blur(14px);cursor:pointer}.profile-pro-body{margin:-72px 25px 0;position:relative}.profile-pro-head{display:grid;grid-template-columns:150px 1fr auto;gap:25px;align-items:end}.profile-pro-avatar-wrap{position:relative;width:150px;height:150px}.profile-pro-avatar{width:150px;height:150px;border-radius:42px;border:7px solid rgba(255,255,255,.96);display:grid;place-items:center;font-size:60px;font-weight:900;color:white;background:linear-gradient(145deg,#2f7cff,#58c7ff 60%,#07101f);box-shadow:0 22px 55px rgba(7,16,31,.35)}.profile-online{position:absolute;right:8px;bottom:8px;width:23px;height:23px;border-radius:50%;background:#38d996;border:4px solid white}.profile-pro-identity{padding-bottom:7px}.profile-name-row{display:flex;align-items:center;gap:9px}.profile-name-row h1{font-size:36px;line-height:1.05;margin:0;font-weight:900;letter-spacing:-1.8px}.verified-badge{width:23px;height:23px;border-radius:50%;display:grid;place-items:center;background:#2f7cff;color:white}.profile-handle{margin:7px 0 8px;color:#637089;font-weight:800}.profile-bio-pro{max-width:700px;margin:0;color:#34415a;font-size:15px;line-height:1.55}.profile-meta{display:flex;gap:18px;flex-wrap:wrap;margin-top:11px;color:#5c6a83;font-size:13px;font-weight:750}.profile-meta span{display:inline-flex;align-items:center;gap:5px}.profile-pro-menu{display:flex;gap:8px;align-self:start}.profile-pro-menu button{width:45px;height:45px;border:1px solid rgba(11,18,32,.1);background:rgba(255,255,255,.86);border-radius:15px;display:grid;place-items:center;color:#17243a;cursor:pointer;box-shadow:0 8px 22px rgba(7,16,31,.08)}.profile-stat-row{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:25px 0 14px}.profile-stat-row button{border:1px solid rgba(11,18,32,.08);background:rgba(255,255,255,.82);border-radius:21px;padding:18px 10px;text-align:center;cursor:pointer;box-shadow:0 10px 30px rgba(7,16,31,.07)}.profile-stat-row strong{display:block;font-size:23px}.profile-stat-row span{display:block;margin-top:3px;color:#718097;font-size:11px;font-weight:900;text-transform:uppercase;letter-spacing:.09em}.profile-primary-actions{display:flex;gap:9px;flex-wrap:wrap}.profile-primary-actions button{border:1px solid rgba(11,18,32,.08);background:white;border-radius:14px;padding:11px 18px;display:inline-flex;align-items:center;gap:7px;font-weight:850;cursor:pointer;box-shadow:0 8px 20px rgba(7,16,31,.06)}.profile-primary-actions .profile-follow{border:0;color:white;background:linear-gradient(135deg,#2f7cff,#58c7ff);box-shadow:0 10px 28px rgba(47,124,255,.28)}.profile-highlights{display:flex;gap:24px;padding:25px 0 19px;overflow:auto}.profile-highlights button{border:0;background:none;display:flex;flex-direction:column;align-items:center;gap:7px;min-width:74px;cursor:pointer;color:#17243a}.highlight-circle{width:70px;height:70px;border-radius:50%;display:grid;place-items:center;background:linear-gradient(145deg,#07101f,#2f7cff);color:#58c7ff;border:3px solid white;box-shadow:0 7px 22px rgba(7,16,31,.2)}.highlight-circle.add{background:white;color:#2f7cff;border:2px dashed #2f7cff}.profile-highlights small{font-size:12px;font-weight:850}.profile-tools-card{display:flex;justify-content:space-between;align-items:center;gap:20px;padding:19px 21px;margin:5px 0 23px;border-radius:23px;color:white;background:linear-gradient(110deg,#07101f,#143e84);box-shadow:0 18px 45px rgba(7,16,31,.2)}.tool-kicker{display:block;color:#58c7ff;font-size:10px;font-weight:900;letter-spacing:.14em;margin-bottom:4px}.profile-tools-card strong{display:block;font-size:18px}.profile-tools-card p{margin:4px 0 0;color:#b9c9e5;font-size:12px}.profile-tools-card button{white-space:nowrap;border:1px solid rgba(255,255,255,.2);background:rgba(255,255,255,.1);color:white;border-radius:12px;padding:10px 14px;display:flex;align-items:center;gap:6px;font-weight:800;cursor:pointer}.profile-tabs{display:flex;border-top:1px solid rgba(11,18,32,.08);border-bottom:1px solid rgba(11,18,32,.08);margin-bottom:3px;overflow:auto}.profile-tabs button{flex:1;min-width:90px;border:0;background:none;padding:18px 9px;display:flex;justify-content:center;align-items:center;gap:7px;color:#8290a5;font-weight:850;cursor:pointer;position:relative}.profile-tabs button.active{color:#2f7cff}.profile-tabs button.active:after{content:"";position:absolute;left:20%;right:20%;bottom:-1px;height:3px;border-radius:4px;background:#2f7cff}.profile-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:4px;margin-top:4px}.profile-grid-item{aspect-ratio:1;position:relative;overflow:hidden;background:#e9eef7;cursor:pointer;border:0;padding:0}.profile-grid-item img,.profile-grid-item video{width:100%;height:100%;object-fit:cover;display:block;transition:transform .35s ease}.profile-grid-item:hover img,.profile-grid-item:hover video{transform:scale(1.05)}.grid-overlay{position:absolute;right:9px;bottom:9px;color:white;background:rgba(7,16,31,.65);padding:6px 8px;border-radius:10px;font-size:11px;font-weight:800;display:flex;align-items:center;gap:4px;backdrop-filter:blur(8px)}.grid-text-post{height:100%;display:grid;place-items:center;padding:24px;background:linear-gradient(135deg,#07101f,#205ab2);color:white}.grid-text-post p{font-size:16px;line-height:1.4;font-weight:700;text-align:center}.profile-empty-grid{grid-column:1/-1;min-height:280px;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;color:#718097;padding:35px}.profile-empty-grid h2{color:#17243a;margin:12px 0 5px}.profile-empty-grid p{margin:0 0 16px;max-width:450px}.profile-empty-grid button{border:0;background:#2f7cff;color:white;border-radius:12px;padding:10px 15px;font-weight:800;cursor:pointer}.profile-toast{position:fixed;z-index:120;left:50%;bottom:92px;transform:translateX(-50%);background:#07101f;color:white;padding:12px 18px;border-radius:14px;box-shadow:0 16px 40px rgba(7,16,31,.3);font-size:13px;font-weight:750}.cv-modal-backdrop{position:fixed;inset:0;z-index:110;background:rgba(3,8,18,.62);backdrop-filter:blur(15px);display:grid;place-items:center;padding:18px}.cv-modal{width:min(560px,100%);max-height:90vh;overflow:auto;border:1px solid rgba(255,255,255,.13);border-radius:26px;background:linear-gradient(145deg,#0a1427,#112b59);color:white;box-shadow:0 35px 100px rgba(0,0,0,.4);padding:24px}.cv-modal.light{background:#fff;color:#0b1220}.cv-modal-head{display:flex;align-items:center;justify-content:space-between;gap:15px;margin-bottom:18px}.cv-modal-head h2{margin:0;font-size:23px}.cv-close{border:0;background:rgba(255,255,255,.1);color:inherit;width:38px;height:38px;border-radius:12px;display:grid;place-items:center;cursor:pointer}.cv-setting-list{display:grid;gap:8px}.cv-setting{width:100%;border:1px solid rgba(255,255,255,.1);background:rgba(255,255,255,.06);color:inherit;border-radius:16px;padding:15px;display:flex;align-items:center;gap:13px;text-align:left;cursor:pointer}.cv-setting strong{display:block}.cv-setting small{display:block;opacity:.65;margin-top:3px}.cv-setting.danger{color:#ff9ba7}.cv-form{display:grid;gap:9px}.cv-form label{font-size:12px;font-weight:850;opacity:.8;margin-top:4px}.cv-form input,.cv-form textarea{font:inherit;border:1px solid #d9e0eb;border-radius:13px;padding:12px 13px;outline:none}.cv-form textarea{min-height:105px;resize:vertical}.cv-save{border:0;background:linear-gradient(135deg,#2f7cff,#58c7ff);color:white;border-radius:13px;padding:12px;font-weight:850;margin-top:6px;cursor:pointer}.cv-people{display:grid;gap:7px}.cv-person{display:flex;align-items:center;gap:11px;padding:11px;border-radius:14px;background:rgba(255,255,255,.06)}.cv-person-avatar{width:40px;height:40px;border-radius:13px;display:grid;place-items:center;background:linear-gradient(145deg,#2f7cff,#58c7ff);font-weight:900}.cv-person small{display:block;opacity:.7;margin-top:2px}.profile-settings-page{display:grid;grid-template-columns:repeat(2,1fr);gap:12px;padding-top:18px}.profile-settings-page button{border:1px solid rgba(11,18,32,.08);background:white;border-radius:18px;padding:18px;display:flex;align-items:center;gap:13px;text-align:left;cursor:pointer;box-shadow:0 8px 22px rgba(7,16,31,.05)}.profile-settings-page .danger{color:#d94d5c}.profile-settings-page strong{display:block}.profile-settings-page small{display:block;color:#718097;margin-top:3px}@media(max-width:700px){.profile-pro{padding-bottom:78px}.profile-pro-cover{height:195px;border-radius:0 0 25px 25px}.profile-pro-body{margin:-48px 14px 0}.profile-pro-head{grid-template-columns:88px 1fr;gap:14px;align-items:end}.profile-pro-avatar-wrap,.profile-pro-avatar{width:88px;height:88px}.profile-pro-avatar{border-width:5px;border-radius:26px;font-size:35px}.profile-online{width:17px;height:17px;right:1px;bottom:2px;border-width:3px}.profile-pro-menu{grid-column:1/-1;position:absolute;right:0;top:100px}.profile-pro-menu button{width:38px;height:38px}.profile-name-row h1{font-size:24px}.profile-bio-pro{font-size:13px}.profile-meta{font-size:11px;gap:9px}.profile-stat-row{gap:5px}.profile-stat-row button{padding:12px 5px;border-radius:15px}.profile-stat-row strong{font-size:18px}.profile-stat-row span{font-size:9px}.profile-primary-actions button{flex:1;justify-content:center;padding:10px 8px;font-size:12px}.profile-highlights{gap:13px}.highlight-circle{width:58px;height:58px}.profile-tools-card{padding:15px;border-radius:18px}.profile-tools-card p{display:none}.profile-tools-card strong{font-size:14px}.profile-grid{gap:2px}.profile-tabs button{min-width:70px;font-size:10px}.profile-tabs button span{display:none}.profile-settings-page{grid-template-columns:1fr}}`}</style>
    <div className="profile-pro-cover"><div className="profile-cover-grid" /><div className="profile-cover-orb" /><button className="profile-cover-more" onClick={() => setMoreOpen(true)} aria-label="Cover options"><MoreHorizontal size={21} /></button></div>
    <div className="profile-pro-body">
      <div className="profile-pro-head"><div className="profile-pro-avatar-wrap"><div className="profile-pro-avatar">{(profile?.display_name || profile?.username || "C").charAt(0).toUpperCase()}</div><span className="profile-online" /></div><div className="profile-pro-identity"><div className="profile-name-row"><h1>{profile?.display_name || "Convogram User"}</h1><span className="verified-badge"><Check size={14} strokeWidth={3} /></span></div><p className="profile-handle">@{profile?.username || "user"}</p><p className="profile-bio-pro">{profile?.bio || "Creating moments, sharing ideas and connecting with my people on Convogram."}</p><div className="profile-meta"><span><Link2 size={14} /> {profile?.website || `convogram.social/${profile?.username || "user"}`}</span><span><Sparkles size={14} /> Creator</span></div></div><div className="profile-pro-menu"><button onClick={() => setEditOpen(true)} aria-label="Edit profile"><Settings size={18} /></button><button onClick={() => { navigator.clipboard?.writeText(`${window.location.origin}/profile/${profile?.username || "user"}`); toast("Profile link copied"); }} aria-label="Share profile"><Share2 size={18} /></button><button onClick={() => setMoreOpen(true)} aria-label="More options"><MoreHorizontal size={18} /></button></div></div>
      <div className="profile-stat-row"><button onClick={() => setActive("posts")}><strong>{stats?.postsCount || 0}</strong><span>Posts</span></button><button onClick={() => openPeople("followers")}><strong>{stats?.followersCount || 0}</strong><span>Followers</span></button><button onClick={() => openPeople("following")}><strong>{stats?.followingCount || 0}</strong><span>Following</span></button><button onClick={() => setActive("liked")}><strong>{likesCount}</strong><span>Likes</span></button></div>
      <div className="profile-primary-actions"><button className="profile-follow" onClick={toggleFollow}><UserPlus size={17} /> {following ? "Following" : "Follow"}</button><button onClick={onMessage}><MessageCircle size={17} /> Message</button><button onClick={onCreatePost}><Plus size={17} /> Post</button><button onClick={onCreateMoment}><Camera size={17} /> Moment</button></div>
      <div className="profile-highlights"><button onClick={() => toast("Highlight creation ready for media selection")}><span className="highlight-circle add"><Plus size={22} /></span><small>New</small></button><button onClick={() => toast("Moments highlight opened")}><span className="highlight-circle"><Sparkles size={22} /></span><small>Moments</small></button><button onClick={() => toast("Creator highlight opened")}><span className="highlight-circle"><Camera size={22} /></span><small>Creator</small></button><button onClick={() => toast("Community highlight opened")}><span className="highlight-circle"><Users size={22} /></span><small>Community</small></button></div>
      <div className="profile-tools-card"><div><span className="tool-kicker">CREATOR CENTER</span><strong>Professional dashboard</strong><p>Insights, content tools, monetization and audience controls.</p></div><button onClick={() => toast("Creator dashboard connected to your account")}><Sparkles size={16} /> Open</button></div>
      <div className="profile-tabs">{tabs.map(({ id, label, icon: Icon }) => <button key={id} className={active === id ? "active" : ""} onClick={() => setActive(id)}><Icon size={18} /><span>{label}</span></button>)}</div>
      {active === "settings" ? <div className="profile-settings-page"><button onClick={() => setEditOpen(true)}><Settings size={20} /><span><strong>Edit profile</strong><small>Update your public profile</small></span></button><button onClick={() => toast(profile?.is_private ? "Your profile is private" : "Your profile is public")}><Lock size={20} /><span><strong>Privacy</strong><small>{profile?.is_private ? "Private profile" : "Public profile"}</small></span></button><button onClick={() => openPeople("followers")}><Users size={20} /><span><strong>Connections</strong><small>Followers and following</small></span></button><button onClick={() => toast("Account security controls opened")}><Sparkles size={20} /><span><strong>Security</strong><small>Password and account protection</small></span></button><button className="danger" onClick={onLogout}><X size={20} /><span><strong>Log out</strong><small>Sign out of this account</small></span></button></div> : <><div className="profile-grid">{displayPosts.length ? displayPosts.map((post) => <button className="profile-grid-item" key={post.id} onClick={() => toggleSave(post.id)} title={savedIds.has(post.id) ? "Remove from Saved" : "Save post"}>{post.media_url ? (post.media_type === "video" ? <video src={post.media_url} muted playsInline /> : <img src={post.media_url} alt={post.caption || "Convogram post"} />) : <div className="grid-text-post"><p>{post.caption || "Convogram post"}</p></div>}{savedIds.has(post.id) && <span className="grid-overlay"><Bookmark size={13} fill="currentColor" /> Saved</span>}</button>) : <div className="profile-empty-grid"><Sparkles size={30} /><h2>{active === "posts" ? "Share your first post" : active === "shorts" ? "Your Shorts will appear here" : active === "saved" ? "Nothing saved yet" : active === "liked" ? "Nothing liked yet" : "No tagged posts yet"}</h2><p>{active === "saved" ? "Save posts from Convogram and they will stay here privately." : active === "liked" ? "Posts you like will be collected here." : active === "tagged" ? "Posts where you are tagged will appear here." : "Build your profile with photos, videos and moments."}</p>{active === "posts" && <button onClick={onCreatePost}>Create post</button>}</div>}</div>{active === "saved" && <div className="profile-private-note"><Lock size={13} /> Saved posts are private to you</div>}</>}
    </div>
    {editOpen && <div className="cv-modal-backdrop" onClick={() => setEditOpen(false)}><div className="cv-modal light" onClick={(e) => e.stopPropagation()}><div className="cv-modal-head"><h2>Edit profile</h2><button className="cv-close" onClick={() => setEditOpen(false)}><X size={18} /></button></div><form className="cv-form" onSubmit={saveProfile}><label>Display name</label><input value={edit.display_name} onChange={(e) => setEdit({ ...edit, display_name: e.target.value })} required /><label>Username</label><input value={edit.username} onChange={(e) => setEdit({ ...edit, username: e.target.value.toLowerCase().replace(/\s/g, "") })} required /><label>Bio</label><textarea value={edit.bio} onChange={(e) => setEdit({ ...edit, bio: e.target.value })} maxLength={500} /><label>Website</label><input value={edit.website} onChange={(e) => setEdit({ ...edit, website: e.target.value })} placeholder="https://..." /><button className="cv-save" disabled={saving}>{saving ? "Saving..." : "Save changes"}</button></form></div></div>}
    {peopleMode && <div className="cv-modal-backdrop" onClick={() => setPeopleMode("")}><div className="cv-modal" onClick={(e) => e.stopPropagation()}><div className="cv-modal-head"><h2>{peopleMode === "followers" ? "Followers" : "Following"}</h2><button className="cv-close" onClick={() => setPeopleMode("")}><X size={18} /></button></div><div className="cv-people">{people.length ? people.map((person) => <div className="cv-person" key={person.id}><div className="cv-person-avatar">{(person.display_name || person.username || "C").charAt(0).toUpperCase()}</div><div><strong>{person.display_name || person.username}</strong><small>@{person.username}</small></div></div>) : <p>No {peopleMode} yet.</p>}</div></div></div>}
    {moreOpen && <div className="cv-modal-backdrop" onClick={() => setMoreOpen(false)}><div className="cv-modal" onClick={(e) => e.stopPropagation()}><div className="cv-modal-head"><h2>Profile options</h2><button className="cv-close" onClick={() => setMoreOpen(false)}><X size={18} /></button></div><div className="cv-setting-list"><button className="cv-setting" onClick={() => { setMoreOpen(false); setActive("settings"); }}><Settings size={19} /><span><strong>Settings</strong><small>Account, privacy and logout</small></span></button><button className="cv-setting" onClick={() => { setMoreOpen(false); navigator.clipboard?.writeText(`${window.location.origin}/profile/${profile?.username || "user"}`); toast("Profile link copied"); }}><Share2 size={19} /><span><strong>Share profile</strong><small>Copy your public profile link</small></span></button></div></div></div>}
    {notice && <div className="profile-toast">{notice}</div>}
  </section>;
}
