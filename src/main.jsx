import { useEffect, useRef, useState } from "react";
import { createRoot } from "react-dom/client";
import { Bell, Camera, Compass, Home, MessageCircle, Plus, Search, Settings, Sparkles, User, Users, X, Zap, SlidersHorizontal } from "lucide-react";
import { supabase } from "./lib/supabase";
import { getFeed, createPost, deletePost, likePost, unlikePost, addComment, isPostLikedByUser } from "./lib/posts";
import { getMomentsForFeed, createMoment } from "./lib/moments";
import { uploadPostMedia, uploadMomentMedia } from "./lib/storage";
import { getShortsForDiscover } from "./lib/shorts";
import { getUnreadNotificationsCount } from "./lib/notifications";
import { getProfileStats } from "./lib/profiles";
import { PostCard } from "./components/PostCard";
import { MomentsRow } from "./components/MomentsRow";
import { ShortsPanel } from "./components/ShortsPanel";
import { MessagesPanel } from "./components/MessagesPanel";
import { CommunitiesPanel } from "./components/CommunitiesPanel";
import { NotificationsPanel } from "./components/NotificationsPanel";
import { ProfilePanel } from "./components/ProfilePanel";
import { FeatureSuite } from "./components/FeatureSuite";
import { EditProfilePage } from "./components/EditProfilePage";
import { SettingsPage } from "./components/SettingsPage";
import { SearchPage } from "./components/SearchPage";
import { ProfileOptionsPage } from "./components/ProfileOptionsPage";
import "./index.css";
const nav = [["home", "Home", Home], ["shorts", "Shorts", Zap], ["messages", "Messages", MessageCircle], ["communities", "Communities", Users], ["profile", "Profile", User]];
const avatar = (profile) => (profile?.display_name || profile?.username || "C").slice(0, 1).toUpperCase();
function App() {
  const [session,setSession]=useState(null),[profile,setProfile]=useState(null),[viewedProfile,setViewedProfile]=useState(null),[booting,setBooting]=useState(true),[authMode,setAuthMode]=useState("login"),[active,setActiveState]=useState("home"),[profileTab,setProfileTab]=useState("posts");
  const activeRef=useRef("home"),navigationStackRef=useRef([]),ignoreNextPopRef=useRef(false),notificationsHistoryRef=useRef(false);
  function setActive(nextPage){if(!nextPage||nextPage===activeRef.current)return;navigationStackRef.current.push(activeRef.current);activeRef.current=nextPage;setActiveState(nextPage);window.history.pushState({convogram:true,page:nextPage},"",window.location.href)}
  function goBack(){const previous=navigationStackRef.current.pop();if(!previous)return;activeRef.current=previous;setActiveState(previous);ignoreNextPopRef.current=true;window.history.back()}
  const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[username,setUsername]=useState(""),[displayName,setDisplayName]=useState(""),[error,setError]=useState(""),[notice,setNotice]=useState(""),[posts,setPosts]=useState([]),[moments,setMoments]=useState([]),[shorts,setShorts]=useState([]),[liked,setLiked]=useState({}),[loadingFeed,setLoadingFeed]=useState(false),[composer,setComposer]=useState(null),[caption,setCaption]=useState(""),[file,setFile]=useState(null),[publishing,setPublishing]=useState(false),[notifications,setNotifications]=useState(false),[featureSuite,setFeatureSuite]=useState(false),[unread,setUnread]=useState(0),[stats,setStats]=useState({postsCount:0,followersCount:0,followingCount:0});
  function openNotifications(){if(notificationsHistoryRef.current)return;notificationsHistoryRef.current=true;setNotifications(true);window.history.pushState({convogram:true,overlay:"notifications"},"",window.location.href)}
  function closeNotifications(){if(notificationsHistoryRef.current)window.history.back();else setNotifications(false)}
  useEffect(()=>{window.history.replaceState({convogram:true,root:true},"",window.location.href);window.history.pushState({convogram:true,root:true},"",window.location.href);const pop=()=>{if(notificationsHistoryRef.current){notificationsHistoryRef.current=false;setNotifications(false);return}if(ignoreNextPopRef.current){ignoreNextPopRef.current=false;return}const previous=navigationStackRef.current.pop();if(previous){activeRef.current=previous;setActiveState(previous)}else window.history.pushState({convogram:true,root:true},"",window.location.href)};window.addEventListener("popstate",pop);return()=>window.removeEventListener("popstate",pop)},[]);
  async function loadProfile(id){if(!supabase||!id)return;const {data}=await supabase.from("profiles").select("*").eq("id",id).single();if(data)setProfile(data)}
  async function refresh(){if(!session?.user?.id||!supabase)return;setLoadingFeed(true);try{const [feed,ms,ps,nu]=await Promise.all([getFeed(40),getMomentsForFeed(session.user.id,30),getProfileStats(session.user.id),getUnreadNotificationsCount(session.user.id)]);setPosts(feed||[]);setMoments(ms||[]);if(ps)setStats(ps);setUnread(nu||0);const m={};await Promise.all((feed||[]).map(async p=>{m[p.id]=await isPostLikedByUser(p.id,session.user.id)}));setLiked(m)}catch(e){setError(e.message||"Could not load Convogram.")}finally{setLoadingFeed(false)}}
  useEffect(()=>{let alive=true,subscription;async function boot(){if(!supabase){setError("Supabase is not configured.");setBooting(false);return}const {data}=await supabase.auth.getSession();if(!alive)return;setSession(data.session);if(data.session){setEmail(data.session.user.email||"");await loadProfile(data.session.user.id)}setBooting(false)}boot();if(supabase){const a=supabase.auth.onAuthStateChange(async(_e,next)=>{setSession(next);setEmail(next?.user?.email||"");if(next)await loadProfile(next.user.id);else{setProfile(null);setViewedProfile(null)}});subscription=a.data?.subscription}return()=>{alive=false;subscription?.unsubscribe()}},[]);
  useEffect(()=>{if(session?.user?.id)refresh()},[session?.user?.id]);
  useEffect(()=>{if(active==="shorts"&&session?.user?.id)getShortsForDiscover(30,0).then(x=>setShorts(x||[])).catch(e=>setError(e.message||"Could not load Shorts."))},[active,session?.user?.id]);
  async function authenticate(e){e.preventDefault();setError("");setNotice("");if(!email||!password){setError("Enter your email and password.");return}if(authMode==="login"){const {error:x}=await supabase.auth.signInWithPassword({email,password});if(x)setError(x.message);return}if(!username.trim()||!displayName.trim()){setError("Enter your name and username.");return}const {data,error:x}=await supabase.auth.signUp({email,password,options:{data:{username:username.trim().toLowerCase(),display_name:displayName.trim()}}});if(x)setError(x.message);else if(!data.session)setNotice("Account created. Check your email to confirm your account.")}
  async function logout(){if(supabase)await supabase.auth.signOut();activeRef.current="home";setActiveState("home");navigationStackRef.current=[];setPosts([]);setMoments([]);setProfile(null);setViewedProfile(null)}
  function openComposer(t){setComposer(t);setCaption("");setFile(null)}function closeComposer(){setComposer(null);setCaption("");setFile(null)}
  async function publish(){if(!session?.user?.id||(!caption.trim()&&!file))return;setPublishing(true);try{if(composer==="post"){let url=null,type="text";if(file){const u=await uploadPostMedia(file,session.user.id);url=u.url;type=u.mediaType}const c=await createPost(session.user.id,caption.trim(),url,type);setPosts(x=>[{...c,profiles:profile,likes:[{count:0}],comments:[{count:0}]},...x])}else{if(!file)throw new Error("Choose a photo or video for a Moment.");const u=await uploadMomentMedia(file,session.user.id);const c=await createMoment(session.user.id,u.url,u.mediaType,caption.trim());setMoments(x=>[{...c,profiles:profile,moment_views:[{count:0}],moment_likes:[{count:0}]},...x])}closeComposer()}catch(e){setError(e.message||"Publishing failed.")}finally{setPublishing(false)}}
  async function toggleLike(post){try{const w=!!liked[post.id];if(w)await unlikePost(post.id,session.user.id);else await likePost(post.id,session.user.id);setLiked(x=>({...x,[post.id]:!w}));setPosts(x=>x.map(i=>i.id===post.id?{...i,likes:[{count:Math.max(0,(i.likes?.[0]?.count||0)+(w?-1:1))}]}:i))}catch(e){setError(e.message||"Could not update like.")}}
  async function comment(post,text){return addComment(post.id,session.user.id,text)}async function removePost(post){try{await deletePost(post.id);setPosts(x=>x.filter(i=>i.id!==post.id))}catch(e){setError(e.message||"Could not delete post.")}}
  if(booting)return <div className="boot"><div className="brand-mark">C</div><h1>Convogram</h1><p>Loading your social world…</p></div>;
  if(!session)return <div className="auth"><div className="auth-card"><div className="brand-row"><div className="brand-mark">C</div><div><b>Convogram</b><span>Everything social, together.</span></div></div><div className="auth-copy"><small>THE SOCIAL SUPERAPP</small><h1>{authMode==="login"?"Welcome back.":"Create your Convogram."}</h1><p>Post, chat, call, discover Shorts, follow people and build communities from one account.</p></div><form onSubmit={authenticate}>{authMode==="signup"&&<><label>Display name<input value={displayName} onChange={e=>setDisplayName(e.target.value)}/></label><label>Username<input value={username} onChange={e=>setUsername(e.target.value)}/></label>}<label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="alert error">{error}</div>}{notice&&<div className="alert">{notice}</div>}<button className="primary">{authMode==="login"?"Log in":"Create account"}</button></form><button className="switch" onClick={()=>setAuthMode(m=>m==="login"?"signup":"login")}>{authMode==="login"?"New to Convogram? Create an account":"Already have an account? Log in"}</button></div></div>;
  const openAccount=(account)=>{if(!account?.id)return;setViewedProfile({...account});setProfileTab("posts");setActive("view-profile")};
  const openOwnProfile=()=>{setViewedProfile(null);setProfileTab("posts");setActive("profile")};
  const openSaved=()=>{setViewedProfile(null);setProfileTab("saved");setActive("profile")};
  return <div className="app" style={{overflowX:"hidden"}}><header className="topbar"><button className="brand-button" onClick={()=>setActive("home")}><span className="brand-mark small">C</span><b>Convogram</b></button><div className="top-search"/><div className="top-actions"><button className="top-create" onClick={()=>openComposer("post")}><Plus size={20}/><span>Post</span></button><button onClick={()=>setActive("search")}><Search size={20}/></button><button onClick={()=>setFeatureSuite(true)}><SlidersHorizontal size={20}/></button><button className="notification" onClick={openNotifications}><Bell size={20}/>{unread>0&&<i>{unread>9?"9+":unread}</i>}</button><button onClick={openOwnProfile}><span className="avatar mini">{avatar(profile)}</span></button></div></header>{notifications&&<NotificationsPanel userId={session.user.id} onClose={()=>{closeNotifications();refresh()}}/>}<main className="layout"><aside className="sidebar"><div className="side-profile"><span className="avatar">{avatar(profile)}</span><div><b>{profile?.display_name||"You"}</b><small>@{profile?.username||"user"}</small></div></div>{nav.map(([id,label,Icon])=><button key={id} className={active===id?"active":""} onClick={()=>id==="profile"?openOwnProfile():setActive(id)}><Icon size={20}/><span>{label}</span>{id==="messages"&&<em>Chat</em>}</button>)}<div className="sidebar-divider"/><button className="create-nav" onClick={()=>openComposer("post")}><Plus size={20}/><span>Create post</span></button><button onClick={()=>setActive("search")}><Search size={20}/><span>Search</span></button><button onClick={()=>setFeatureSuite(true)}><SlidersHorizontal size={20}/><span>All features</span></button><button onClick={()=>setActive("settings")}><Settings size={20}/><span>Settings</span></button><button className="logout" onClick={logout}>↪<span>Log out</span></button></aside><section className="content">{error&&<div className="global-alert">{error}<button onClick={()=>setError("")}><X size={15}/></button></div>}{active==="home"&&<><section className="panel first-panel"><div className="section-head"><div><small>24 HOURS</small><h2>Moments</h2></div><div className="section-actions"><button onClick={()=>openComposer("moment")}><Plus size={16}/> Add Moment</button><button onClick={()=>openComposer("post")}><Plus size={16}/> Post</button></div></div><MomentsRow moments={moments} currentUserId={session.user.id} onAddMoment={()=>openComposer("moment")} loading={false} error={null}/></section><section className="panel"><div className="section-head"><div><small>YOUR FEED</small><h2>For you</h2></div><div className="section-actions"><button onClick={()=>openComposer("post")}><Plus size={16}/> Create post</button><button onClick={refresh}><Compass size={16}/> Refresh</button></div></div>{loadingFeed?<div className="empty">Loading your feed…</div>:posts.length?posts.map(p=><PostCard key={p.id} post={p} currentUserId={session.user.id} isLiked={!!liked[p.id]} onLike={()=>toggleLike(p)} onUnlike={()=>toggleLike(p)} onComment={t=>comment(p,t)} onDelete={()=>removePost(p)} likeCount={p.likes?.[0]?.count||0} commentCount={p.comments?.[0]?.count||0}/>):<div className="empty"><Sparkles size={28}/><h3>Your feed starts here.</h3><p>Share the first thing your people will see.</p><button className="primary" onClick={()=>openComposer("post")}>Create your first post</button></div>}</section></>}{active==="search"&&<SearchPage currentUserId={session.user.id} onBack={goBack} onOpenProfile={openAccount}/>} {active==="shorts"&&<section className="page"><div className="page-head"><div><small>SHORT VIDEO</small><h1>Shorts</h1><p>Short videos, sounds and creators.</p></div><button onClick={()=>getShortsForDiscover(30,0).then(x=>setShorts(x||[]))}><Zap size={18}/> Refresh</button></div><ShortsPanel shorts={shorts} userId={session.user.id}/></section>} {active==="messages"&&<section className="page"><MessagesPanel userId={session.user.id}/></section>} {active==="communities"&&<section className="page"><CommunitiesPanel userId={session.user.id}/></section>} {active==="profile"&&<ProfilePanel key="own-profile" userId={session.user.id} profile={profile} stats={stats} initialTab={profileTab} onCreatePost={()=>openComposer("post")} onCreateMoment={()=>openComposer("moment")} onMessage={()=>setActive("messages")} onEdit={()=>setActive("profile-options")} onProfileUpdated={setProfile} onPeople={(type,account)=>type==="profile"&&openAccount(account)}/>} {active==="view-profile"&&viewedProfile&&<ProfilePanel key={`viewed-${viewedProfile.id}`} userId={session.user.id} profile={viewedProfile} stats={undefined} initialTab={profileTab} onCreatePost={()=>openComposer("post")} onCreateMoment={()=>openComposer("moment")} onMessage={()=>setActive("messages")} onEdit={()=>setActive("profile-options")} onProfileUpdated={setProfile} onPeople={(type,account)=>type==="profile"&&openAccount(account)}/>} {active==="profile-options"&&<ProfileOptionsPage profile={profile} onBack={goBack} onViewProfile={openOwnProfile} onSaved={openSaved} onEditProfile={()=>setActive("edit-profile")} onSettings={()=>setActive("settings")} onLogout={logout}/>} {active==="settings"&&<SettingsPage profile={profile} userId={session.user.id} email={email} onBack={goBack} onEditProfile={()=>setActive("edit-profile")} onLogout={logout} onProfileUpdated={setProfile}/>} {active==="edit-profile"&&<EditProfilePage profile={profile} userId={session.user.id} onBack={goBack} onProfileUpdated={setProfile}/>}</section></main><nav className="mobile-nav">{nav.map(([id,label,Icon])=><button key={id} className={active===id?"active":""} onClick={()=>id==="profile"?openOwnProfile():setActive(id)}><Icon size={21}/><span>{label}</span></button>)}</nav>{composer&&<div className="modal-backdrop" onClick={closeComposer}><div className="composer" onClick={e=>e.stopPropagation()}><div className="composer-head"><div><small>{composer==="post"?"CREATE":"24 HOURS"}</small><h2>{composer==="post"?"New post":"New Moment"}</h2></div><button onClick={closeComposer}><X/></button></div><textarea value={caption} onChange={e=>setCaption(e.target.value)} placeholder={composer==="post"?"What's happening?":"Add a caption…"}/><label className="file-picker"><Camera size={18}/><span>{file?file.name:"Add photo or video"}</span><input type="file" accept="image/*,video/*" onChange={e=>setFile(e.target.files?.[0]||null)}/></label><button className="primary publish" disabled={publishing||(!caption.trim()&&!file)} onClick={publish}>{publishing?"Publishing…":"Publish"}</button></div></div>}{featureSuite&&<FeatureSuite userId={session.user.id} onClose={()=>setFeatureSuite(false)}/>}</div>;
}
createRoot(document.getElementById("root")).render(<App/>);